'use client';

import { PackageItem } from '@/app/_type/package';

type Props = {
  packageItems: PackageItem[];
};

export default function PackageItemsZone({ packageItems }: Props) {
  return <div>PackageItemsZone</div>;
}
